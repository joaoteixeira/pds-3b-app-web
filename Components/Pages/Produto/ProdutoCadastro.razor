@page "/produtos/novo"
@rendermode InteractiveServer

@using AppWeb.Models;
@inject ProdutoDAO produtoDAO;
@inject NavigationManager Navigation;

<PageTitle>Produtos</PageTitle>

<h1 class="text-xl font-semibold text-gray-800 dark:text-gray-100 mb-6">Novo Produto</h1>

@if (erro != string.Empty)
{
  <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4" role="alert">
    <strong class="font-bold">Erro!</strong>
    <span class="block sm:inline">@erro</span>
  </div>
}

<EditForm Model="novoProduto" OnSubmit="Salvar">
  <div class="mb-4">
    <label for="nome" class="block text-sm font-medium text-gray-700">Nome</label>
    <input @bind-value="novoProduto.Nome" type="text" id="nome"
      class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm p-2" />
  </div>
  <div class="mb-4">
    <label for="quantidade" class="block text-sm font-medium text-gray-700">Quantidade</label>
    <input @bind-value="novoProduto.Quantidade" type="number" id="quantidade"
      class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm p-2" />
  </div>
  <div class="mb-4">
    <label for="valor" class="block text-sm font-medium text-gray-700">Valor</label>
    <input @bind-value="novoProduto.Valor" type="text" id="valor"
      class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm p-2" />
  </div>

  <div class="mb-4">
    <label for="descricao" class="block text-sm font-medium text-gray-700">Descrição</label>
    <textarea id="descricao"
      class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm p-2">@novoProduto.Descricao</textarea>
  </div>
  <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded">Salvar</button>
</EditForm>

@code {

  private string erro = string.Empty;

  [SupplyParameterFromForm]
  private Produto novoProduto { get; set; } = new();

  protected override void OnInitialized()
  {
  }

  private void Salvar()
  {
    try
    {
      produtoDAO.Inserir(novoProduto);

      Navigation.NavigateTo("/produtos");
    }
    catch (Exception ex)
    {
      erro = $"Erro ao salvar o produto: {ex.Message}";
    }

  }
}